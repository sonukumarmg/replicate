build:
  gpu: false
  python_version: "3.10"
  python_packages:
    - "torch==2.1.2"
    - "torchvision==0.16.2"
    - "numpy==1.26.4"
    - "timm"
    - "einops"
    - "Pillow"
    - "cog"       # <-- Added cog here
    # If you still need replicate for something else, you can keep it
    # - "replicate"
predict: "predict.py:Predictor"

